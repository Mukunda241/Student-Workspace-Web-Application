package com.studentworkspace.config;

import org.springframework.context.annotation.Configuration;

/**
 * Application-level configuration beans.
 * Note: RestTemplate is already defined in CorsConfig.java
 */
@Configuration
public class AppConfig {
    // No beans needed here currently
}
